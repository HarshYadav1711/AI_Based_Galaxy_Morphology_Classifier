[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "galaxy-morphology"
version = "1.0.0"
description = "AI-assisted galaxy morphology analysis: local PyTorch workflows, multi-task heads, robustness, and explainability."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Galaxy Morphology Classifier contributors" }]
dependencies = [
  "torch>=2.0.0",
  "torchvision>=0.15.0",
  "numpy>=1.24.0",
  "pillow>=9.5.0",
  "matplotlib>=3.7.0",
  "scikit-learn>=1.3.0",
  "astropy>=5.3.0",
  "requests>=2.31.0",
  "tqdm>=4.65.0",
  "pandas>=2.0.0",
  "pyyaml>=6.0.0",
  "grad-cam>=1.5.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "ruff>=0.4.0",
  "black>=24.0.0",
  "pre-commit>=3.7.0",
]
demo = [
  "streamlit>=1.28.0",
]

[project.scripts]
galaxy-train = "galaxy_morphology.training.cli:main"
galaxy-infer = "galaxy_morphology.inference.cli:main"
galaxy-sample-data = "galaxy_morphology.data.sample_download:main"
galaxy-benchmark = "galaxy_morphology.evaluation.benchmark_cli:main"
galaxy-trust-report = "galaxy_morphology.explainability.trust_report_cli:main"
galaxy-scientific-eval = "galaxy_morphology.evaluation.scientific_cli:main"
galaxy-dataset-study = "galaxy_morphology.analysis.dataset_study_cli:main"
galaxy-al-export = "galaxy_morphology.active_learning.export_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests", "scripts", "app"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
"scripts/*" = ["E402"]
"train.py" = ["E402"]
"inference.py" = ["E402"]
"download_sample_data.py" = ["E402"]
"example_usage.py" = ["E402"]
"model.py" = ["E402"]
"data_loader.py" = ["E402"]
"app/streamlit_app.py" = ["E402"]
